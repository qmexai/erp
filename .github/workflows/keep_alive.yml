name: "Render Keep-Alive"

on:
  schedule:
    - cron: '*/14 * * * *'
  workflow_dispatch:

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Health Endpoint
        run: |
          curl -A "GitHub-Keep-Alive/1.0" -L -I https://erp-e1ax.onrender.com/health/
